# 2026-04-16 Daily Memory

## 易经占卜网站 (i-chingstudio.cc) — 修复记录

### 今日解决的问题

1. **@/ 路径别名解析失败 (Cloudflare Pages)**
   - 根因：tsconfig.json 的 paths 配置在 Cloudflare Pages Root directory 设置下失效
   - 修复：在 next.config.ts 添加显式 webpack alias 配置
   - commit: c04758dd

2. **NEXT_PUBLIC_API_URL 环境变量缺失**
   - 根因：.env.local 被 .gitignore 排除，未 commit 到 GitHub
   - 修复：在 Cloudflare Pages Dashboard 的 Environment variables 添加该变量
   - 值：`https://yi-jing-divination-4h6y.onrender.com`

3. **后端 /api/divination/compute 返回 500**
   - 根因1：remarkTable.js 中有两条记录的 yaoIndex="ALL"（不是数字），导致 int("ALL") ValueError
   - 根因2：data_loader.py 的 js_to_json 函数在转换单引号时把双引号字符串内容（如 tiger's）也破坏了
   - 修复1：get_remark 中捕获 yaoIndex 转换异常
   - 修复2：重写 js_to_json 函数，先保护双引号字符串再转单引号
   - commits: 739071ee, 4db86bf2

4. **Google 登录后用户名不显示**
   - 修复：AuthProvider 添加监听 AUTH_LOGIN_COMPLETE 事件
   - commit: 739071ee

5. **Callback 页面硬编码中文**
   - 修复：添加 useLang hook 显示对应语言文本
   - commit: 739071ee

6. **本卦/之卦拼音显示**
   - 修复：GUA_NAME_PAIR 样式添加 white-space:nowrap 确保拼音不换行
   - commit: 739071ee (style fix)

### 平台部署机制

- **Cloudflare Pages**: GitHub push 自动触发部署（无需手动）
- **Render**: 需要手动点击 "Manual Deploy" 才能部署新 commit
- 教训：每次代码修改后必须 Render 手动部署才能生效

### 待确认

- 卦象生成功能是否已正常工作（用户尚未确认）
- 产品名称英文翻译（name_en/description_en）需要部署后访问 /api/credits/fix-product-translations 修复
- 积分从 103 变 3 的问题（可能是 Render Free 实例休眠导致数据丢失）

### 项目架构

- 前端：Next.js，部署在 Cloudflare Pages
- 后端：Python FastAPI，部署在 Render (yi-jing-divination-4h6y.onrender.com)
- 数据库：SQLite，在 Render 实例上
- 环境变量 NEXT_PUBLIC_API_URL 需要在 Cloudflare Pages 设置
