# 2026-04-15 Daily Memory

## 项目背景
- Owner: Kathy (KathyXJ, ou_1649cad74e8c2856a545868ed429963d)
- GitHub: KathyXJ/yi-jing-divination (PAT已配置)
- 前端: https://i-chingstudio.cc (Cloudflare Pages)
- 后端: https://yi-jing-divination-4h6y.onrender.com

## 今日完成修复

### Cloudflare Pages 部署配置
- **Root directory**: 空（或 /）
- **Build command**: `cd web/frontend && npm install && npx next build`
- **Build output**: `web/frontend/out`
- 问题: 之前设置的 Root directory = web/frontend 导致路径冲突（cd web/frontend 实际找 web/frontend/web/frontend）

### 后端 500 错误根因
- `get_db()` 缺少 `@asynccontextmanager` 装饰器
- 导致 `async with get_db()` 失败，返回 "'async_generator' object does not support the asynchronous context manager protocol"
- **必须手动在 Render Dashboard 点击 Manual Deploy**

### 前端构建错误
- `@/` 路径别名找不到模块（@/lib/auth, @/lib/i18n, @/lib/api）
- 根因: `token` 类型是 `string | null`，但 `interpretWithAI` 期望 `string | undefined`
- 修复: `handleInterpret` 中 `token ?? undefined`

### AI 解读按钮点击无反应
- 根因: `interpretWithAI` 没有发送 Authorization header
- 修复: 在 `page.tsx` 的 `handleInterpret` 中获取 `token` 并传给 API

### 定价页问题
- Badge 逻辑错误: 标准包($9.9)应标 Most Popular，月订阅($19.9)应标 Best Value
- 价格显示 `toFixed(0)` 截断问题: $9.9 → $9，$19.9 → $20
- 修复: `toFixed(2).replace(/\.?0+$/, '')`
- 注册赠送硬编码"永久有效"，应读取 `valid_days` 显示

### 首页重复按钮
- Navbar 和 page.tsx header 都有语言切换+登录按钮
- 删除 page.tsx header 中的重复按钮

### i18n 默认语言
- 默认语言改为英文
- 语言偏好持久化到 localStorage

### 64卦拼音数据
- 从 Kathy 提供的「易经 64 卦拼音.md」添加到 `web/backend/data/doubleGuaTable.js`
- commit: `2e0ae95a` (1 line, 内容压缩)
- 后端 cast API 返回的 `ben_gua.pinyin` 和 `zhi_gua.pinyin` 需要 Render 部署后才能生效

### Git submodule 问题
- `yi-jing-divination-temp` 被错误注册为 submodule
- 修复: `git rm -rf yi-jing-divination-temp && git commit`
- wrangler.toml 文件也删除了（Pages 不支持）

### 英文翻译
- Hexagram text → "Hexagram text"
- Changing Lines → "Changing Lines"
- 本卦/之卦 → "卦名 pinyin Hexagram" (如 "临 lín Hexagram")

## 待完成
1. **Render 后端手动部署** — 必须 Kathy 在 Render Dashboard 点击 Manual Deploy
2. **Cloudflare Pages 自动部署** — 应已自动触发
3. **AI 解读测试** — 需要登录后测试，API 返回 401 说明未登录

## 定价框架（已确认）
- 标准积分包: $9.9/50积分，永久有效，Most Popular
- 月度订阅: $19.9/200积分/月，每月补充，Best Value
- 注册赠送: 3积分，7天内有效
